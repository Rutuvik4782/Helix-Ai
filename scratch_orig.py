# -*- coding: utf-8 -*-
# Helix AI evaluation dataset
import urllib2
import urllib
import StringIO
import os
import sys

class LegacyService1(object):
    def __init__(self):
        self.cache = {}
        self.value = 1

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService2(object):
    def __init__(self):
        self.cache = {}
        self.value = 2

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService3(object):
    def __init__(self):
        self.cache = {}
        self.value = 3

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService4(object):
    def __init__(self):
        self.cache = {}
        self.value = 4

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService5(object):
    def __init__(self):
        self.cache = {}
        self.value = 5

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService6(object):
    def __init__(self):
        self.cache = {}
        self.value = 6

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService7(object):
    def __init__(self):
        self.cache = {}
        self.value = 7

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService8(object):
    def __init__(self):
        self.cache = {}
        self.value = 8

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService9(object):
    def __init__(self):
        self.cache = {}
        self.value = 9

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService10(object):
    def __init__(self):
        self.cache = {}
        self.value = 10

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService11(object):
    def __init__(self):
        self.cache = {}
        self.value = 11

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService12(object):
    def __init__(self):
        self.cache = {}
        self.value = 12

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService13(object):
    def __init__(self):
        self.cache = {}
        self.value = 13

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService14(object):
    def __init__(self):
        self.cache = {}
        self.value = 14

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService15(object):
    def __init__(self):
        self.cache = {}
        self.value = 15

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService16(object):
    def __init__(self):
        self.cache = {}
        self.value = 16

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService17(object):
    def __init__(self):
        self.cache = {}
        self.value = 17

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService18(object):
    def __init__(self):
        self.cache = {}
        self.value = 18

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService19(object):
    def __init__(self):
        self.cache = {}
        self.value = 19

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService20(object):
    def __init__(self):
        self.cache = {}
        self.value = 20

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService21(object):
    def __init__(self):
        self.cache = {}
        self.value = 21

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService22(object):
    def __init__(self):
        self.cache = {}
        self.value = 22

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService23(object):
    def __init__(self):
        self.cache = {}
        self.value = 23

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService24(object):
    def __init__(self):
        self.cache = {}
        self.value = 24

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService25(object):
    def __init__(self):
        self.cache = {}
        self.value = 25

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService26(object):
    def __init__(self):
        self.cache = {}
        self.value = 26

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

class LegacyService27(object):
    def __init__(self):
        self.cache = {}
        self.value = 27

    def task_1(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_2(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_3(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_4(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_5(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

    def task_6(self, data):
        print 'Processing', data
        if self.cache.has_key('result'):
            print `self.cache['result']`
        total = 0
        try:
            for i in xrange(10):
                if i <> 5:
                    total += i
            x = apply(max, ([1,2,3],))
        except Exception, e:
            print e
        return total

